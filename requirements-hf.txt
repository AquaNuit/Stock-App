# Slim runtime requirements for Hugging Face Spaces (free tier, 1 GB RAM).
# Heavy ML libraries (xgboost, lightgbm, prophet, torch) are excluded
# to stay within memory/disk limits. They can be added back for paid tiers.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
sqlalchemy>=2.0
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4
statsmodels>=0.14
httpx>=0.27
yfinance>=0.2.40
apscheduler>=3.10
